#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int n;
    cin>>n;
    vector<int> a(n);
    vector<int> b(n);
    vector<int> c(n);
    for(int i=0;i<n;++i)
        cin>>c[i];
    for(int i=0;i<n;++i){
        cin>>a[i];
    }
    for(int i=0;i<n;++i)
        cin>>b[i];
    int len=0;
    for(int i=n-1;i>=0;--i){

    }
}
int main()
{
    int t;
    cin >> t;
    while (t--)
        solve();
    return 0;
}
